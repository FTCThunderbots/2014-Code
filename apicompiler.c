// apicompiler.c
// Used only so that the api can be compiled at once

#include "api/api.h"

// RobotC refuses to compile .h files, so this file exists only
// to compile the api at once in a controlled environment
